export interface ProjectView {
  id: string;
  name: string;
  description: string;
  ownerId: string;
  ownerName: string;
  teamId: string;
  teamName: string;
  created: string;
  active: boolean;
}
